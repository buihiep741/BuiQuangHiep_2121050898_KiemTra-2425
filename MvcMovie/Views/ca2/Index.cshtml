@model IEnumerable<MvcMovie.Models.ca2>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.diachi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tuoi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.diachi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tuoi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ten">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ten">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ten">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
